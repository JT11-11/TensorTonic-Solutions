- pd.merge(left_df,right_df, on = on, how = how) --&gt; this returns smth like this: 
   key_column   name  score

0           2    Bob     90

1           3  Carol     85
- right, left is just the data, on is the key and how is the method! simple!