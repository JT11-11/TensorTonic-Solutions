- this is an extension to the previous lecture which extends the indexing of columns.
- to drop a index, use .reset_index()

- in addition, u can do .reset_index(drop = True) which can delete the index column